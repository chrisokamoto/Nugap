<%= form_for(@tipo_analise) do |f| %>
  <% if @tipo_analise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_analise.errors.count, "erro") %>:</h2>

      <ul>
      <% @tipo_analise.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo %><br>
    <%= f.text_field :tipo %>
  </div>
  <div class="actions">
    <%= f.submit "Salvar", :class => 'btn btn-info' %>
  </div>
<% end %>
